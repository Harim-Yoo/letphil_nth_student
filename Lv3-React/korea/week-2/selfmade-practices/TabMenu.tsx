// ==========================================
// 3. Tab Menu
// ==========================================
/**
 * Components: Tabs (Parent), TabButton (Child)
 * Requirements:
 * - Parent stores: activeTabId (number or string)
 * - Child receives: tab name, id, isActive (boolean), onClick function
 * - Child action: Clicking the tab updates the parent's activeTabId state
 */
const TabButton = (props: any) => { return <></>; };
export const Tabs = () => { return <div></div>; };