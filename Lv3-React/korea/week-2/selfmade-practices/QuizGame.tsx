// ==========================================
// 8. Quiz Game (점수 및 다음 질문으로 넘어가기)
// ==========================================
/**
 * Components: QuizManager (Parent), QuestionCard (Child)
 * Requirements:
 * - Parent stores: currentQuestionIndex (number), score (number)
 * - Child receives: question text, choices array, onAnswer function
 * - Child action: Clicking a choice passes the selected answer back to parent. Parent checks if correct, updates score, and increments question index.
 */
const QuestionCard = (props: any) => { return <></>; };
export const QuizManager = () => { return <div></div>; };
